export default {
  arrowParens: 'always', // Include parentheses around a sole arrow function parameter.
  bracketSpacing: true, // Print spaces between brackets in object literals.
  bracketSameLine: false, // Put the > of a multi-line HTML (HTML, JSX, Vue, Angular) element at the end of the last line.
  endOfLine: 'lf', // Make sure that your entire git repository only contains Linux-style line endings in files.
  htmlWhitespaceSensitivity: 'strict', // Specify the global whitespace sensitivity for HTML.
  jsxSingleQuote: false, // Use single quotes instead of double quotes in JSX.
  proseWrap: 'preserve', // To have Prettier wrap prose to the print width - always, to leave as-is - preserve, to never wrap - never
  printWidth: 140, // Specify the line length that the printer will wrap on.
  quoteProps: 'as-needed', // Change when properties in objects are quoted.
  semi: true, // Print semicolons at the ends of statements.
  singleQuote: true, // Use single quotes instead of double quotes.
  tabWidth: 2, // Specify the number of spaces per indentation-level.
  trailingComma: 'all', // Print trailing commas wherever possible in multi-line comma-separated syntactic structures.
  useTabs: false, // Indent lines with tabs instead of spaces.
  embeddedLanguageFormatting: 'auto', // Format embedded code if Prettier can automatically identify it.
  singleAttributePerLine: true, // Enforce single attribute per line in HTML, Vue and JSX.
};
